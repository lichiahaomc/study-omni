{
  "name": "study-omni",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "StudyOmni 多模态教学智能体 · 粤港澳大湾区 AI Coding 创新大赛",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=studyomni --commit-dirty=true",
    "pages:create": "wrangler pages project create studyomni --production-branch=main",
    "tail": "wrangler pages deployment tail",
    "whoami": "wrangler whoami",
    "test": "node tests/providers.test.mjs && node tests/ratelimit.test.mjs && node tests/subjects.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
